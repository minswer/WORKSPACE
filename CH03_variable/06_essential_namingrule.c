/*  **무조건 외워야함. 시험에도 무조건 나올 예정정**
1. 변수(Variable), 함수 등 명명 규칙
    -1. 알파벳(대문자, 소문자), 숫자, 특수문자(_)만 사용가능
    -2. 숫자로는 시작 불가.(ex. 1ABC(x), ABC1(o))
    -3. 대문자와 소문자는 다르게 인식(ex. ABC != ABc != abc)
    -4. 예약어는 사용 불가(예약어 : 프로그래밍 언어에서 이미 사용하고 있는 키워드 = ex. main, printf, sizeof ...)
    
2. 네이밍 케이스(합성어 표현)
    ex. age, student + number
    ex. C : Class(객체)
        M : Method(함수)
        V : Variable(변수)
    -1. 파스칼 케이스(각 단어의 첫 글자 대문자)   -> Age, StudentNumber      => JAVA(객체), Python(객체체)
    -2. 카멜 케이스(첫 단어의 첫 글자 소문자자)     -> age, studentNumber    => c언어(함수, 변수), JAVA(함수, 변수), JSON
    -3. 스네이크 케이스(소문자 사용, 각 단어 사이에 _ 사용해서 이음)      -> age, student_number     => Python(함수, 변수)
    -4. 케밥 케이스(소문자 사용, 각 단어 사이에 - 사용해서 이음)     -> age, student-number     => HTML, CSS = 웹디자인에서 주로 사용

    ex. JAVA
    StudentNumber : C
    studentNumber() : M
    studentNumber : V

*/